import { StyleSheet } from 'react-native';
import { baseBgColor } from '../../constants/styles';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: baseBgColor,
    width: '100%',
    alignItems: 'stretch',
  },
});
