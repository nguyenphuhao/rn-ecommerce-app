import { StyleSheet } from 'react-native';
import { primaryColor, baseBgColor } from '../../constants/styles';

export default StyleSheet.create({
  container: {
    backgroundColor: primaryColor,
    padding: 10,
  },
  searchItem: {
    backgroundColor: baseBgColor,
  },
});
